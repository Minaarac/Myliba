export class UserModel {

    public id?: number;
    public name?: string;
    public surname?: string;
    public email?: string;

    //constructor(obj?: Partial<UserModel>) {
      //  if (obj) {
            //Object.assign(this, obj)
       // }
   // }
}